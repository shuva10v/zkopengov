{
  "name": "zk-opengov-contracts",
  "version": "1.0.0",
  "description": "ZK Private Voting smart contracts for Polkadot OpenGov",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run script/deploy.ts --network localhost",
    "deploy:testnet": "hardhat run script/deploy.ts --network polkadotHubTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@parity/hardhat-polkadot": "^0.2.7",
    "@parity/resolc": "^1.0.0",
    "@types/node": "^20.0.0",
    "circomlibjs": "^0.1.7",
    "hardhat": "^2.19.0",
    "snarkjs": "^0.7.6",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
